namespace descriptor-constructs
from /core/types import int as target, int as same
function main;
  value int = 1
  print; value is a target
  print; target is int
  print; target is same
  print; value.type is target
