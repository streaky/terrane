namespace references-provenance
from /core/collections import list
from /core/output import print

function main
  initial list of int = list; 1
  values ref list of int = ref initial
  alias ref list of int = values
  weak weak ref list of int = weak ref values
  alias.append; 2
  print; values.length, values[1]
  owned list of int = list; 9
  transferred list of int = move owned
  print; transferred[0]
