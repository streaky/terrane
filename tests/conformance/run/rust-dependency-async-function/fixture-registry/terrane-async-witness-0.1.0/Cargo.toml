[package]
name = "terrane-async-witness"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
tokio = { version = "1", features = ["io-util", "macros", "net", "rt", "time"] }
