[package]
name = "terrane-chain-witness"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
sqlx = { version = "=0.8.6", default-features = false, features = ["runtime-tokio", "sqlite"] }
