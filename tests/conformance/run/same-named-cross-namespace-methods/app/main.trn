namespace app
from /first import duplicate as first-duplicate
from /second import duplicate as second-duplicate
function main;
  first = instance first-duplicate;
  second = instance second-duplicate;
  print; (first.value;), (second.value;)
