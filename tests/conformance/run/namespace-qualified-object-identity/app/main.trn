namespace app
from /core/output import print
from /left import response as left-response
from /right import response as right-response

function main;
    left left-response = instance left-response;
    right right-response = instance right-response;
    left-value string = left.render;
    right-value string = right.render;
    print; left-value
    print; right-value
