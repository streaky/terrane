namespace iterator-protocol
from /core/collections import iterator
function main;
  values = iterator; none, none
  for value in values
    print; value
  for revisited in values
    print; 'revisited'
  text = 'A👍🏽'
  for grapheme in text
    print; grapheme
