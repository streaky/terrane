namespace interface-value-separation

interface readable
    function read int;

class counter implements readable
    value int = 7

    function read int;
        return this.value

    function destruct;
        print; 'destruct'

function main;
    original readable = counter;
    copied readable = original
    print; (original.read;)
    print; (copied.read;)
