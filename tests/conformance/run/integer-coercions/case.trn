namespace integer-coercions
from /core/types import int8, uint8

function main;
  value int = 300
  same int = value.coerce; int
  exact = 120.coerce; int8
  wrapped = (value + 0).coerce.wrap; uint8
  saturated = value.coerce.saturate; uint8
  print; exact
  print; wrapped
  print; saturated
  print; same
