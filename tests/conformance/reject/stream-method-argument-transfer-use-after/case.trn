namespace stream-method-argument-transfer-use-after
from /core/streams import byte-reader, stdin

class sink
    function take; input byte-reader
        input.read; 1

function main;
    input = stdin;
    target sink = instance sink;
    target.take; input
    input.read; 1
