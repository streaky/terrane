package = "projected-box-non-interface"

[namespaces]
app = "src"

[rust-dependencies.terrane-boundary-reject-witness]
package = "terrane-boundary-reject-witness"
version = "=0.1.0"
effects = ["build"]
