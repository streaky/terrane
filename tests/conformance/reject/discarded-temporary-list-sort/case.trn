namespace discarded-temporary-list-sort
from /core/collections import list, map

function main;
  values map of string, list of int = map; alpha=(list; 2, 1)
  for pair in values.entries;
    pair.value.sort;
