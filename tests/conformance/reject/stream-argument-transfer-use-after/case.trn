namespace stream-argument-transfer-use-after
from /core/streams import byte-reader, stdin

function consume; input byte-reader
    input.read; 1

function main;
    input = stdin;
    consume; input
    input.read; 1
