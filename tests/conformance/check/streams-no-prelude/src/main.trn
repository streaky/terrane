namespace streams-no-prelude
from /core/streams import stdin, stdout, stderr
function main;
    input = stdin;
    input.close;
    output = stdout;
    output.close;
    errors = stderr;
    errors.close;
